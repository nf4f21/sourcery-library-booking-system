# Setting up local development

### Step 1

Run `npm install`. 

This only needs to be run if the `node_modules` 
folder is missing from the project root directory, or if 
`package.json` or `package-lock.json` have been updated.

### Step 2

Run `npm start`.

### Step 3

Visit `http://localhost:3000/` in your browser.
